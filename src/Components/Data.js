const Data = [
    {
      bg:"#26030a",    
      data:[
          {// The Middle one
              position:[0.5, -0.2, 0],
              rotation:[0, 0, 0.715],
              factor: 0.75 + Math.random() * 2,
              direction: Math.random() < 0.5 ? -1 : 1,
              size:0.4,
              scale:0.4
          }, 
          {// The Middle right
            position:[0.5, -.2, 1],
            rotation:[0, 0, 0.715],
            factor: 0.75 + Math.random() * 2,
            direction: Math.random() < 0.5 ? -1 : 1,
            size:0.2,
            scale:0.1
          }, 
          { // The Middle Left
            position:[0.5, -.2, -1],
            rotation:[0, 0, 0.715],
            factor: 0.75 + Math.random() * 2,
            direction: Math.random() < 0.5 ? -1 : 1,
            size:0.2,
            scale:0.3
          },
          { // The top right
              position:[0, .4, 1],
              rotation:[0, 0, 0.715],
              factor: 0.75 + Math.random() * 2,
              direction: Math.random() < 0.5 ? -1 : 1,
              size:0.2,
              scale:0.3
          },
          { // The Middle Top
            position:[0, .4, 0],
            rotation:[0, 0, 0.715],
            factor: 0.75 + Math.random() * 2,
            direction: Math.random() < 0.5 ? -1 : 1,
            size:0.2,
            scale:0.3
        },
          { // The Top Left
              position:[0, 0.4, -1],
              rotation:[0, 0, 0.715],
              factor: 0.75 + Math.random() * 2,
              direction: Math.random() < 0.5 ? -1 : 1,
              size:0.2,
              scale:0.4
            },
            { // The Bottom Right
              position:[0, -1, 1],
              rotation:[0, 0, 0.715],
              factor: 0.75 + Math.random() * 2,
              direction: Math.random() < 0.5 ? -1 : 1,
              size:0.2,
              scale:0.3
            },
            { // The bottom left
              position:[0, -1, -1],
              rotation:[0, 0, 0.715],
              factor: 0.75 + Math.random() * 2,
              direction: Math.random() < 0.5 ? -1 : 1,
              size:0.2,
              scale:0.3
            },
            { // The middle bottom
              position:[0, -1, 0],
              rotation:[0, 0, 0.715],
              factor: 0.75 + Math.random() * 2,
              direction: Math.random() < 0.5 ? -1 : 1,
              size:0.2,
              scale:0.3
            }
      ]
    },
    {
        bg:"salmon",    
        data:[
            {// The Middle one
                position:[0.5, -0.2, 0],
                rotation:[0, 0, 0.715],
                factor: 0.75 + Math.random() * 2,
                direction: Math.random() < 0.5 ? -1 : 1,
                size:0.5,
                scale:0.4
            }, 
            {// The Middle right
              position:[0.5, -.2, 1],
              rotation:[0, 0, 0.715],
              factor: 0.75 + Math.random() * 2,
              direction: Math.random() < 0.5 ? -1 : 1,
              size:0.1,
              scale:0.1
            }, 
            { // The Middle Left
              position:[0.5, -.2, -1],
              rotation:[0, 0, 0.715],
              factor: 0.75 + Math.random() * 2,
              direction: Math.random() < 0.5 ? -1 : 1,
              size:0.1,
              scale:0.3
            },
            { // The top right
                position:[0, .4, 1],
                rotation:[0, 0, 0.715],
                factor: 0.75 + Math.random() * 2,
                direction: Math.random() < 0.5 ? -1 : 1,
                size:0.1,
                scale:0.3
            },
            { // The Middle Top
              position:[0, .4, 0],
              rotation:[0, 0, 0.715],
              factor: 0.75 + Math.random() * 2,
              direction: Math.random() < 0.5 ? -1 : 1,
              size:0.1,
              scale:0.3
          },
            { // The Top Left
                position:[0, 0.4, -1],
                rotation:[0, 0, 0.715],
                factor: 0.75 + Math.random() * 2,
                direction: Math.random() < 0.5 ? -1 : 1,
                size:0.1,
                scale:0.4
              },
              { // The Bottom Right
                position:[0, -1, 1],
                rotation:[0, 0, 0.715],
                factor: 0.75 + Math.random() * 2,
                direction: Math.random() < 0.5 ? -1 : 1,
                size:0.1,
                scale:0.3
              },
              { // The bottom left
                position:[0, -1, -1],
                rotation:[0, 0, 0.715],
                factor: 0.75 + Math.random() * 2,
                direction: Math.random() < 0.5 ? -1 : 1,
                size:0.1,
                scale:0.3
              },
              { // The middle bottom
                position:[0, -1, 0],
                rotation:[0, 0, 0.715],
                factor: 0.75 + Math.random() * 2,
                direction: Math.random() < 0.5 ? -1 : 1,
                size:0.1,
                scale:0.3
              }
        ]
      },
  ]
  
  export default Data